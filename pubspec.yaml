name: biscuits
version: 2.8.0

description: Service for interacting with the HTTP cookies. A simple and event-based API for handling cookies.
homepage: https://docs.belin.io/biscuits.dart
issue_tracker: https://git.belin.io/cedx/biscuits.dart/issues
repository: https://git.belin.io/cedx/biscuits.dart

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  http_parser: ^3.1.4
  json_annotation: ^3.1.1

dev_dependencies:
  build_runner: ^1.10.7
  build_test: ^1.3.0
  build_web_compilers: ^2.12.2
  coverage: ^0.14.2
  coveralls: ^5.10.0
  json_serializable: ^3.5.1
  test: ^1.15.7
